
#include"logger.hpp"

int main() {
          //appends strings to logger.txt
	Logger::instance().report("test1");
	Logger::instance().report("test2");
	Logger::instance().report("Hello");
	 Logger::instance().report("World");
	 // creates object by referace to logger::instance
	 Logger &lg = Logger::instance();
	 lg.report("Sweet this works");
}
